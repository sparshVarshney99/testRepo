name: Build and Deploy JAR

on:
  push:
    tags:
      - 'v*'  # Trigger only when a version tag is pushed (e.g., v1.0.0)

jobs:
  build:
    name: Build and Upload to Release
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Set up Java 21
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '21'

      - name: Grant execute permission to Gradle wrapper
        run: chmod +x ./gradlew

      - name: Build with Gradle
        run: ./gradlew build

      - name: Upload JAR to GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          files: build/libs/*.jar
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  deploy:
    name: Deploy to Application
    needs: build
    runs-on: ubuntu-latest

    steps:
      - name: Download JAR from GitHub Release
        uses: robinraju/release-downloader@v1.8
        with:
          repository: ${{ github.repository }}
          tag: ${{ github.ref_name }}
          fileName: '*.jar'
          out-file-path: ./app/
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Deploy Application
        run: |
          echo "Deploying the application using the JAR..."
          ls ./app/
          # Example: java -jar ./app/your-app.jar
          # Replace this with your actual deployment logic
